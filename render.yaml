services:
  - type: web
    name: trust-global-bank
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: node server.ts
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
